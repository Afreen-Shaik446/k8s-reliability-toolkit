# Example Secret. Values below are base64 placeholders — replace them.
# In real clusters prefer external-secrets or a sealed-secrets workflow
# over committing secrets to git.
apiVersion: v1
kind: Secret
metadata:
  name: demo-secrets
type: Opaque
data:
  # echo -n "replace-me" | base64
  api-key: cmVwbGFjZS1tZQ==
  jwt-secret: cmVwbGFjZS1tZQ==
